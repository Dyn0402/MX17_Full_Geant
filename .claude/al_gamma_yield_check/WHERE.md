# Moved

Results, figures, and slides for this task live in `docs/al_gamma_yield_check/`
(scripts in `analysis/al_gamma_yield_check/`). This directory keeps only the
original task spec, `HANDOFF.md`.
